import styled from "styled-components";
import { TextStyles, Colors } from "../../Theme";

const H3 = styled.h3`
  ${TextStyles.Bundler(TextStyles.H3)};
  color: ${Colors.Brand.Text};
`;

export default H3;